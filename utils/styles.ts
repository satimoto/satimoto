import { StyleSheet } from "react-native"

export default StyleSheet.create({
    matchParent: {
        flex: 1
    },
    center: {
        justifyContent: "center",
        alignItems: "center"
    }
})